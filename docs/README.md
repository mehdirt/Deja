# Docs index

Entry point for project docs beyond the root-level README.md, DESIGN.md, ROADMAP.md, and CONCEPT.md.

- `plans/` — dated implementation plans (e.g. GTM launch, feature rollouts).
- `ops/` — operational runbooks (release process, incident handling, store submission).
- `corpus-reference/` — reference material for prompt corpora / test fixtures used across the classifier, search, and similarity modules.

This suite is a stub as of 2026-08-04 — populate each section as the corresponding work happens rather than backfilling speculatively.
